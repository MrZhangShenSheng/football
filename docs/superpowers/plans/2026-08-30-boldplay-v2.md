# 阶梯出票卡 v2 实施计划（两档制+多池引擎+逐场三池卡）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 spec v1.2——保底 HAD 4串11 + 翻身多池引擎两档制，逐场三池玩法卡（TTG/HAFU 聚合+α 纠偏+双行推荐+分歧旗），legacy 双轨过渡一个月。

**Architecture:** freq_band.py 加三个纯函数（ttg_agg / hafu_alpha+hafu_agg / pools_card）作地基；boldplay.py 加 `--structure=new|legacy`（new=两档/legacy=旧三档独立文件）；settle 适配双形状；SKILL v5.5 + preference.json 纪律文本；freq_backtest 加结构对照维度。出票卡文本渲染带**可读性硬规范**（大哥 2026-08-30 要求：最后的出票卡可阅读性一定要好）。

**Tech Stack:** Python 3 标准库 + 已有依赖（requests 不新增用途），无 pytest 之外新工具；测试 = freq_band/boldplay `--selftest` 模式 + engine/tests/ pytest 薄壳（仓库惯例）。

**Spec:** `docs/2026-08-30-boldplay-v2-design.html`（v1.2——§4.2 聚合公式 / §4.3 双行推荐+分歧旗+低置信 / §4.4 legacy 分离与回测范围 / §4.5 改动清单含涟漪 / §7 ADR D1-D12）

## Global Constraints

- 主入口 `python engine/scripts/run.py <子命令>`；其余脚本在 `engine/scripts/`
- 枚举/常量禁止魔法值：`MONTHLY_UPSET_CAP = 40`、`DIVERGENCE_FLAG_PP = 5`、`LOW_CONF_N = 100`、`POOL_CODES = ("had","hhad","crs","ttg","hafu")` 等顶层定义
- 函数 docstring 标注开发者 `sszhang`
- q 源=真实频率（freq-band 铁律 8 不动）：CRS 用现有 `shifted_q`/`freq_legs` 原样，TTG/HAFU 聚合只做分桶/分解，不引入 DC
- α 纠偏：全局系数起步（02-results half 字段，实测存量 115 场），落盘 `engine/cache/hafu_alpha.json` 缓存；selftest 断言 `alpha("dd") >= 1`
- 跨池腿约束：翻身引擎**同场最多 1 腿**；混串木桶（含 CRS/HAFU ≤4 关）
- legacy 双轨：`--structure=legacy` 输出 `{date}-boldplay-legacy.json` 独立文件，防同日覆盖
- 月度彩票预算：`MONTHLY_UPSET_CAP = 40` 元（复用 boldplay 现有 monthly_spend 聚合机制，按 upset 档 cost 累计），连续 4 轮翻身 0 回款自动降半仓位并在卡面提示
- **出票卡可读性硬规范（大哥要求）**：见 Task 4 的渲染规范块——逐腿一行、关键列对齐、摘要置顶、emoji 分档、与 v5.4.2 出票核对单同款式
- 预测 JSON `matches[]` 新增 `pools` 字段：corpus 透传不展开；attribute v1 对 pools 仅记录不归因
- TDD：每任务 RED→GREEN→commit；全量回归 `cd engine && python3 -m pytest tests -q`（当前基线 178 passed）

---

### Task 1: freq_band.ttg_agg 总进球聚合

**Files:**
- Modify: `engine/scripts/freq_band.py`（文件末尾追加函数与 selftest 块）
- Test: 同文件 `--selftest`；集成后补 `engine/tests/test_freq_band.py` 冒烟（追加一个测试函数）

**Interfaces:**
- Consumes: `shifted_q(blob, lam)` 的输出 `{比分"i:j": q}`（归一化 dict）
- Produces: `ttg_agg(q_map: dict) -> dict` 返回 `{"s0".."s7": float}`（s7=7+ 并桶，Σ≈1）。Task 3 pools_card 消费。

- [ ] **Step 1: 追加失败 selftest**（freq_band.py 若无 `--selftest` 入口则同时建：`if __name__ == "__main__": import sys; selftest() if "--selftest" in sys.argv`，若已有入口只追加断言块）

```python
def _selftest_ttg():
    q = {"1:0": 0.20, "0:1": 0.15, "2:2": 0.10, "3:3": 0.05, "0:0": 0.30, "2:0": 0.12, "4:3": 0.08}
    # 归一化fixture Σ=1.00
    out = ttg_agg(q)
    assert abs(sum(out.values()) - 1.0) < 1e-9
    assert abs(out["s0"] - 0.30) < 1e-9          # 0:0
    assert abs(out["s1"] - 0.35) < 1e-9          # 1:0 + 0:1
    assert abs(out["s4"] - 0.10) < 1e-9          # 2:2
    assert abs(out["s6"] - 0.05) < 1e-9          # 3:3
    assert abs(out["s7"] - 0.08) < 1e-9          # 4:3 → 7球并入s7(=7+)
    assert abs(out["s2"] - 0.12) < 1e-9          # 2:0
    print("[selftest] ttg_agg OK")
```

- [ ] **Step 2: RED** — `cd engine/scripts && python3 freq_band.py --selftest` → FAIL（NameError: ttg_agg）

- [ ] **Step 3: 实现**

```python
def ttg_agg(q_map: dict) -> dict:
    """比分分布 → 体彩总进球8档 {s0..s7}（s7=7+并桶；'胜其他'等长尾并入s7保守）。
    纯分桶不改形状（spec §4.2）。开发者 sszhang"""
    out = {f"s{k}": 0.0 for k in range(8)}
    for s, q in q_map.items():
        if ":" not in s:
            out["s7"] += q
            continue
        h, a = (int(x) for x in s.split(":"))
        out[f"s{min(h + a, 7)}"] += q
    return out
```

- [ ] **Step 4: GREEN + pytest 冒烟**

Run: `python3 freq_band.py --selftest` → `[selftest] ttg_agg OK`；在 `engine/tests/test_freq_band.py` 追加：

```python
def test_ttg_agg_buckets():
    from freq_band import ttg_agg
    out = ttg_agg({"1:0": 0.6, "2:2": 0.4})
    assert abs(out["s1"] - 0.6) < 1e-9 and abs(out["s4"] - 0.4) < 1e-9
    assert abs(sum(out.values()) - 1.0) < 1e-9
```

Run: `cd engine && python3 -m pytest tests/test_freq_band.py -q` → PASS

- [ ] **Step 5: Commit** — `git add engine/scripts/freq_band.py engine/tests/test_freq_band.py && git commit -m "feat(boldplay-v2): ttg_agg比分分桶聚合(体彩s0-s7对齐)"`

---

### Task 2: hafu_alpha 纠偏系数 + hafu_agg 九键聚合

**Files:**
- Modify: `engine/scripts/freq_band.py`
- Create: `engine/cache/hafu_alpha.json`（运行时产物，随 commit 入库=git凭证）
- Test: selftest 块 + `engine/tests/test_freq_band.py` 追加

**Interfaces:**
- Consumes: `data/02-results/*.json` 的 `matches[].result`（"2-1"）与 `matches[].half`（"0:1"，实测 115 场）；`ROOT` from common
- Produces:
  - `hafu_alpha(results_dir: Path | None = None) -> dict`：返回 `{"alpha": {九键: float}, "n": int}` 并落盘 `engine/cache/hafu_alpha.json`（缓存，存在且 n 不变时复用）
  - `hafu_agg(q_map: dict, p_half: dict, alpha: dict) -> dict`：`p_half={"h","d","a"}` 半场三向经验频率 → 返回九键 `{"hh".."aa": float}` Σ=1
  - `half_three_way(results_dir=None) -> dict`：115 场半场三向频率（p_half 数据源，Task 3 消费）

- [ ] **Step 1: 追加失败 selftest**

```python
def _selftest_hafu():
    from pathlib import Path
    res = hafu_alpha()                       # 真实02-results数据
    assert res["n"] >= 100, f"半场样本不足: {res['n']}"
    assert res["alpha"]["dd"] >= 1.0, f"α(dd)必须≥1(方向性·spec D8): {res['alpha']['dd']}"
    p_half = half_three_way()
    assert abs(sum(p_half.values()) - 1.0) < 1e-6
    # 构造q_map: 纯主胜分布 → FT=(1,0,0) → 九键只剩 xh
    qm = {"2:0": 0.5, "3:1": 0.5}
    alpha1 = {k: 1.0 for k in ("hh","hd","ha","dh","dd","da","ah","ad","aa")}
    out = hafu_agg(qm, p_half, alpha1)
    assert abs(sum(out.values()) - 1.0) < 1e-9
    assert abs(out["hh"] + out["dh"] + out["ah"] - 1.0) < 1e-9   # FT全主胜→xh三键占满
    # 纠偏生效: α(hh)=2 时 hh 翻倍(归一化前)
    alpha2 = dict(alpha1); alpha2["hh"] = 2.0
    out2 = hafu_agg(qm, p_half, alpha2)
    assert out2["hh"] > out["hh"] * 1.5      # 归一化稀释后仍显著抬升
    print("[selftest] hafu_alpha + hafu_agg OK")
```

- [ ] **Step 2: RED** — NameError

- [ ] **Step 3: 实现**

```python
HAFU_KEYS = [f"{x}{y}" for x in "hda" for y in "hda"]
ALPHA_CACHE = ROOT / "engine" / "cache" / "hafu_alpha.json"


def _half_ft_rows(results_dir=None):
    """02-results → [(半场三向idx, 全场三向idx)]（half+result 齐全的场次）。开发者 sszhang"""
    from pathlib import Path
    base = Path(results_dir) if results_dir else ROOT / "data" / "02-results"
    rows = []
    for p in sorted(base.glob("2*.json")):
        try:
            data = json.loads(p.read_text(encoding="utf-8"))
        except (OSError, json.JSONDecodeError):
            continue
        for m in data.get("matches") or []:
            sc, hf = m.get("result"), m.get("half")
            if not (sc and hf and ":" in str(hf)):
                continue
            try:
                hg, ag = (int(x) for x in str(sc).replace("-", ":").split(":"))
                hh_, ha_ = (int(x) for x in str(hf).split(":"))
            except ValueError:
                continue
            tri = lambda a, b: 0 if a > b else (1 if a == b else 2)
            rows.append((tri(hh_, ha_), tri(hg, ag)))
    return rows


def half_three_way(results_dir=None) -> dict:
    """本地半场三向经验频率 {"h","d","a"}（α 与 HAFU 聚合共用数据源）。开发者 sszhang"""
    rows = _half_ft_rows(results_dir)
    n = len(rows)
    if not n:
        return {"h": 1/3, "d": 1/3, "a": 1/3}     # 无数据→均匀退化(低置信)
    c = [0, 0, 0]
    for x, _ in rows:
        c[x] += 1
    return {"h": c[0]/n, "d": c[1]/n, "a": c[2]/n}


def hafu_alpha(results_dir=None) -> dict:
    """观测9键频率 / [P_half×P_FT]全局 → 纠偏系数α（spec §4.2 v1.1·D8）。
    全局口径混合联赛(选择偏差诚实标注于JSON note)；样本0的键α=1.0不纠。
    结果缓存 engine/cache/hafu_alpha.json（n不变复用）。开发者 sszhang"""
    rows = _half_ft_rows(results_dir)
    n = len(rows)
    if ALPHA_CACHE.exists():
        try:
            cached = json.loads(ALPHA_CACHE.read_text(encoding="utf-8"))
            if cached.get("n") == n:
                return cached
        except (OSError, json.JSONDecodeError):
            pass
    obs = {k: 0 for k in HAFU_KEYS}
    ph = [0, 0, 0]; pf = [0, 0, 0]
    for x, y in rows:
        obs[HAFU_KEYS[x * 3 + y]] += 1
        ph[x] += 1; pf[y] += 1
    tri = "hda"
    alpha = {}
    for k in HAFU_KEYS:
        x, y = tri.index(k[0]), tri.index(k[1])
        base = (ph[x] / n) * (pf[y] / n)
        alpha[k] = round((obs[k] / n) / base, 4) if base > 0 and obs[k] >= 5 else 1.0
    out = {"alpha": alpha, "n": n, "ranAt": str(date.today()),
           "note": "全局口径·混合联赛·含选场偏差; obs<5键不纠(α=1)"}
    ALPHA_CACHE.parent.mkdir(parents=True, exist_ok=True)
    ALPHA_CACHE.write_text(json.dumps(out, ensure_ascii=False, indent=1) + "\n", encoding="utf-8")
    return out


def hafu_agg(q_map: dict, p_half: dict, alpha: dict) -> dict:
    """半场三向(经验) × 本场FT三向(q_map聚合) × α → 九键归一（spec §4.2）。
    注意: half_share.json 为 s/ρ_half 标量结构非九键，v1 用本地经验 P_half（侦察确认）。开发者 sszhang"""
    ph_ft = sum(q for s, q in q_map.items() if ":" in s and int(s.split(":")[0]) > int(s.split(":")[1]))
    pa_ft = sum(q for s, q in q_map.items() if ":" in s and int(s.split(":")[0]) < int(s.split(":")[1]))
    p_ft = {"h": ph_ft, "d": max(1 - ph_ft - pa_ft, 0.0), "a": pa_ft}
    raw = {f"{x}{y}": p_half[x] * p_ft[y] * alpha.get(f"{x}{y}", 1.0) for x in "hda" for y in "hda"}
    z = sum(raw.values())
    return {k: v / z for k, v in raw.items()} if z else {k: 1/9 for k in HAFU_KEYS}
```

（`from collections import Counter` 等已有；`json`/`date` 若 freq_band.py 头部未 import 则补。）

- [ ] **Step 4: GREEN** — selftest 两行 OK（含真实数据 `α(dd)≥1` 方向性断言）；pytest 追加：

```python
def test_hafu_agg_normalized():
    from freq_band import hafu_agg
    p_half = {"h": 0.4, "d": 0.35, "a": 0.25}
    alpha = {k: 1.0 for k in ("hh","hd","ha","dh","dd","da","ah","ad","aa")}
    out = hafu_agg({"1:1": 0.5, "2:2": 0.5}, p_half, alpha)
    assert abs(sum(out.values()) - 1.0) < 1e-9
```

- [ ] **Step 5: Commit** — `feat(boldplay-v2): hafu_alpha经验纠偏(α·dd必上修)+hafu_agg九键聚合`

---

### Task 3: pools_card 三池玩法卡（EV+双行推荐+分歧旗+低置信）

**Files:**
- Modify: `engine/scripts/freq_band.py`
- Test: selftest 块 + pytest 追加

**Interfaces:**
- Consumes: Task 1 `ttg_agg`、Task 2 `hafu_agg/half_three_way/hafu_alpha`；`sporttery_matches.json` 单场结构（`crs/ttg/hafu` 池 dict、`league`）；freq_table（联赛映射 `map_league`）
- Produces: `pools_card(m: dict, q_map: dict, form: dict, zh: dict, freq_table: dict) -> dict`，结构：

```python
{"code", "match", "candidates": [ {"pool": "crs|ttg|hafu", "pick", "q", "odds", "ev"} × 最多5 ],
 "rec_base": {...候选引用}, "rec_upset": {...}, "flags": ["low_conf"|"divergence"|...] }
```

Task 4 翻身选腿与 SKILL Step 7 报告消费此结构；预测 JSON `matches[].pools` 存 `candidates+rec_base+rec_upset+flags`。

- [ ] **Step 1: 追加失败 selftest**

```python
def _selftest_pools():
    m = {"code": "周日004", "league": "德乙", "home": "圣保利", "away": "凯泽",
         "crs": {"0:2": 24.0, "1:1": 7.5}, "ttg": {"s2": 3.75, "s3": 3.55},
         "hafu": {"dd": 6.25, "hh": 2.75}}
    ft = {"germany-bundesliga2": Counter({"1:1": 40, "2:2": 20, "__n": 200})}   # 简化模板
    card = pools_card(m, {"0:2": 0.115, "1:1": 0.08}, {}, {}, ft)              # q_map 由调用方 shifted_q 产出
    # 分歧旗: CRS 0:2 q=11.5% vs 市场隐含≈1/24/1.13≈3.7% → 分歧7.8pp>5pp
    assert "divergence" in card["flags"]
    # 双行: rec_base 命中优先(q高) / rec_upset 赔率优先
    assert card["rec_base"]["pool"] != card["rec_upset"]["pool"] or card["rec_base"]["pick"] != card["rec_upset"]["pick"]
    assert all(abs(c["ev"] - (c["q"] * c["odds"] - 1)) < 1e-9 for c in card["candidates"])
    # 低置信: 联赛映射缺失(芬超)
    m2 = {"code": "周一002", "league": "芬超", "home": "赫尔火花", "away": "TPS",
          "crs": {"0:2": 20.0}, "ttg": {"s3": 3.55}, "hafu": {"aa": 5.2}}
    card2 = pools_card(m2, {}, {}, {}, {})
    assert "low_conf" in card2["flags"]
    print("[selftest] pools_card OK")
```

- [ ] **Step 2: RED** — NameError

- [ ] **Step 3: 实现**（要点全写，嵌入 freq_band.py）

```python
DIVERGENCE_FLAG_PP = 5      # |q − 市场隐含| 超此值(百分点)触发分歧旗（spec D12）
MARKET_MARGIN_DIV = 1.13    # 体彩三向去水除数近似
LOW_CONF_N = 100            # 模板样本低置信线（spec D9：不砍池只标旗）


def pools_card(m, q_map, form, zh, freq_table):
    """单场三池玩法卡（spec §4.3 v1.2）：CRS带内top1 + TTG top2 + HAFU top2，
    EV=q×体彩赔率−1；双行推荐（保底=相近带q最高/翻身=相近带赔率最高）；
    CRS分歧旗（平移放大效应·降级第二推荐）；低置信旗（模板缺失/样本<LOW_CONF_N，
    不砍池推荐走结构兜底=抽水低池优先）。开发者 sszhang"""
    lg = map_league(m.get("league", ""))
    blob = freq_table.get(lg) if lg else None
    n_tpl = (blob or {}).get("__n", 0) if blob else 0
    pool_n = (global_pool(freq_table) or {}).get("__n", 0) if freq_table else 0
    low_conf = (n_tpl < LOW_CONF_N and pool_n >= LOW_CONF_N) or "low_conf"
    flags = []
    if n_tpl < LOW_CONF_N:
        flags.append("low_conf")
    # 联赛模板优先, 缺则全局池(freq_legs 同款降级); q_map 已含平移
    q_eff = q_map if q_map else shifted_q(global_pool(freq_table) if freq_table else Counter(), None)
    p_half, alpha_doc = half_three_way(), hafu_alpha()
    alpha = alpha_doc.get("alpha", {})
    ttg_q = ttg_agg(q_eff)
    hafu_q = hafu_agg(q_eff, p_half, alpha)
    cands = []
    crs_top = None
    for s, o in (m.get("crs") or {}).items():
        o = float(o)
        if ":" in s and BAND_DEFAULT[0] <= o <= BAND_DEFAULT[1]:      # 形状带门槛(铁律8)
            c = {"pool": "crs", "pick": s, "q": round(q_eff.get(s, 0.0), 4), "odds": o}
            if crs_top is None or c["q"] > crs_top["q"]:
                crs_top = c
    if crs_top and crs_top["q"] > 0:
        crs_top["ev"] = round(crs_top["q"] * crs_top["odds"] - 1, 4)
        cands.append(crs_top)
    ttg_c = sorted(({"pool": "ttg", "pick": k, "q": round(v, 4), "odds": float(o)}
                    for k, o in (m.get("ttg") or {}).items() for v in [ttg_q.get(k, 0.0)] if v > 0),
                   key=lambda c: -c["q"])[:2]
    hafu_c = sorted(({"pool": "hafu", "pick": k, "q": round(v, 4), "odds": float(o)}
                     for k, o in (m.get("hafu") or {}).items() for v in [hafu_q.get(k, 0.0)] if v > 0),
                    key=lambda c: -c["q"])[:2]
    for c in ttg_c + hafu_c:
        c["ev"] = round(c["q"] * c["odds"] - 1, 4)
    cands.extend(ttg_c + hafu_c)
    if not cands:
        return {"code": m.get("code"), "match": f'{m.get("home")} vs {m.get("away")}',
                "candidates": [], "flags": flags + ["no_candidates"]}
    # CRS 分歧旗（spec D12）: CRS 候选 EV 居首且 |q−市场隐含|>阈值 → 降级第二
    if cands and cands[0]["pool"] == "crs" and crs_top:
        implied = 1.0 / (crs_top["odds"] * MARKET_MARGIN_DIV)
        if (crs_top["q"] - implied) * 100 > DIVERGENCE_FLAG_PP and len(cands) > 1:
            flags.append("divergence")
            cands.append(cands.pop(0))            # 降级到末位=第二推荐
    # 双行推荐: EV 相近带(差<0.1·演示档)内, 保底取q最高/翻身取赔率最高
    evs = [c["ev"] for c in cands]
    band = [c for c in cands if c["ev"] >= max(evs) - 0.1]
    rec_base = max(band, key=lambda c: c["q"])
    rec_upset = max(band, key=lambda c: c["odds"])
    if "low_conf" in flags:                        # 结构兜底: 抽水低池优先(spec D9)
        low_vig = [c for c in band if c["pool"] in ("ttg", "hafu")]
        if low_vig:
            rec_base = max(low_vig, key=lambda c: c["q"])
    return {"code": m.get("code"), "match": f'{m.get("home")} vs {m.get("away")}',
            "candidates": cands, "rec_base": rec_base, "rec_upset": rec_upset, "flags": flags}
```

（`BAND_DEFAULT` 若 freq_band.py 无则定义 `BAND_DEFAULT = (10.0, 28.0)`（桂林-梅州合并带，与 boldplay 现状一致）；实现者以文件内已有常量优先，勿重复定义——发现已有 `BAND` 类常量即复用并在报告注明。）

- [ ] **Step 4: GREEN + pytest**（追加 `test_pools_card_divergence_flag` 用 selftest 同款 fixture 断言 `divergence in flags`）

- [ ] **Step 5: Commit** — `feat(boldplay-v2): pools_card三池卡(EV+双行推荐+分歧旗+低置信兜底)`

---

### Task 4: boldplay --structure 两档制 + 出票卡可读性渲染

**Files:**
- Modify: `engine/scripts/boldplay.py`（main/build_ticket 结构参数 + 新渲染函数）
- Test: `--selftest` 块 + 集成 dry-run

**Interfaces:**
- Consumes: Task 3 `pools_card`（经 freq_band import）；现有 `pick_upset_legs/monthly_spend/budget_gate/cap_multiplier`；`backfill.expand_combos`（4串11 展开已有实现，import 复用）
- Produces:
  - `build_two_tier(odds_day, freq_table, seq, zh, form) -> dict`（新结构票卡 JSON：`{"structure": "new", "date", "seq", "tiers": {"base": {4串11}, "upset": {轮换形状}}, "totalCost", "cards": [pools_card×A/B级场]}`）
  - `render_ticket(ticket) -> str`（**可读性硬规范**文本卡）
  - `MONTHLY_UPSET_CAP = 40` 顶层常量；`upset_month_spend(month)`（扫当月 `*-boldplay*.json` 的 tiers.upset.cost 聚合）

- [ ] **Step 1: 追加失败 selftest**

```python
def _selftest_two_tier():
    fake_day = {"matches": [
        {"code": "周日004", "league": "德乙", "home": "圣保利", "away": "凯泽",
         "had": {"h": 1.72, "d": 3.6, "a": 3.7},
         "crs": {"0:2": 24.0, "1:1": 7.5}, "ttg": {"s2": 3.75}, "hafu": {"dd": 6.25}},
        {"code": "周一002", "league": "芬超", "home": "赫尔火花", "away": "TPS",
         "had": {"h": 1.9, "d": 3.5, "a": 3.15},
         "crs": {"0:2": 20.0}, "ttg": {"s3": 3.55}, "hafu": {"aa": 5.2}},
    ]}
    t = build_two_tier(fake_day, {}, seq=9, zh={}, form={})
    assert t["structure"] == "new"
    base = t["tiers"]["base"]
    assert base["cost"] == 22 and len(base["legs"]) == 4            # 4串11=22元
    assert base["play"] == "had-4串11"
    up = t["tiers"]["upset"]
    assert up["shape"] in ("pool-2x1x3", "pool-4x1")               # seq奇偶轮换
    codes = [l["matchNumStr"] for l in up["legs"]]
    assert len(codes) == len(set(codes))                            # 同场最多1腿(硬约束)
    assert 2 <= up["cost"] <= 8
    assert 24 <= t["totalCost"] <= 30
    txt = render_ticket(t)
    for kw in ("出票核对单", "周日004", "圣保利", "TTG", "│"):     # 可读性规范锚点
        assert kw in txt, kw
    print("[selftest] build_two_tier + render_ticket OK")
```

- [ ] **Step 2: RED**

- [ ] **Step 3: 实现**（核心结构，插 boldplay.py）

```python
MONTHLY_UPSET_CAP = 40          # 翻身月度彩票预算（spec §4.1 note·preference 同步）
from backfill import expand_combos          # 4串11=大小≥2全组合(结算引擎同源)
from freq_band import pools_card, freq_legs


def upset_month_spend(month: str) -> float:
    """当月翻身档累计投入：扫 data/03-predictions/{MM月内}-boldplay*.json 的 tiers.upset.cost。开发者 sszhang"""
    total = 0.0
    for p in PRED_DIR.glob(f"{month}-*-boldplay*.json"):
        try:
            doc = json.loads(p.read_text(encoding="utf-8"))
            total += ((doc.get("tiers") or {}).get("upset") or {}).get("cost") or 0
        except (OSError, json.JSONDecodeError):
            continue
    return total


def build_two_tier(odds_day, freq_table, seq, zh, form):
    """新两档结构（spec §4.1）：保底 HAD 4串11(22元) + 翻身多池引擎(seq轮换)。
    选腿: 保底=现HAD四星三星池; 翻身=各场 pools_card rec_upset 候选(同场≤1腿, 按EV降序)。开发者 sszhang"""
    # 保底: 复用现有 HAD 选腿(现 base 档逻辑抽出的函数或内联重写, 取赔率带1.5~2.4的4腿)
    had_legs = _pick_had_legs(odds_day)                    # 4 腿 [{matchNumStr,match,play,pick,odds}]
    base = {"cost": 22, "legs": had_legs, "play": "had-4串11",
            "bets": [{"legs": list(c), "multiplier": 1} for c in expand_combos(4)],
            "note": "6×2串1+4×3串1+1×4串1 · 中2关回1注2串1"}
    # 三池卡(A/B级场=boldplay现有入池口径)
    cards = [pools_card(m, _q_map_for(m, freq_table, form, zh), form, zh, freq_table)
             for m in odds_day.get("matches", []) if _is_ab(m)]
    # 翻身: seq 奇=跨池2串1×3 / 偶=跨池4串1×N; 腿=各场 rec_upset(同场≤1)
    cand = sorted((c["rec_upset"] for c in cards if c.get("candidates")), key=lambda c: -c["ev"])
    seen, legs = set(), []
    for c in cand:
        code = c.get("code")
        if code in seen:
            continue
        seen.add(code)
        legs.append({"matchNumStr": code, "match": c.get("match"), "play": c["pool"],
                     "pick": c["pick"], "odds": c["odds"], "q": c["q"], "ev": c["ev"]})
    if seq % 2 == 1:                                        # 容错引擎: 3注独立2串1
        n = min(3, len(legs) // 2)
        pairs = [(legs[i*2], legs[i*2+1]) for i in range(n)]
        upset = {"shape": "pool-2x1x3", "cost": n * 2, "legs": legs[:n*2],
                 "bets": [{"legs": [i*2, i*2+1], "multiplier": 1} for i in range(n)],
                 "note": f"{n}注跨池2串1(同场≤1腿)"}
    else:                                                   # 彩票: 跨池4串1×倍数
        m4 = legs[:4]
        mult = cap_multiplier(math.prod(l["odds"] for l in m4), 4, 50.0) if len(m4) == 4 else 0
        upset = {"shape": "pool-4x1", "cost": 2 * mult if mult else 0, "legs": m4,
                 "multiplier": mult, "bets": ([{"legs": [0,1,2,3], "multiplier": mult}] if mult else []),
                 "note": "跨池4串1(木桶≤4关)"}
    if upset["cost"] < 2:                                   # 腿不足关档(铁律8不硬凑)
        upset = {"shape": "closed", "cost": 0, "legs": legs, "note": "翻身候选腿不足·关档"}
    return {"structure": "new", "date": str(date.today()), "seq": seq,
            "tiers": {"base": base, "upset": upset},
            "totalCost": base["cost"] + upset["cost"],
            "cards": cards, "ranAt": str(date.today())}
```

（`_pick_had_legs/_is_ab/_q_map_for` 为对现有 base 选腿逻辑/入池判定/shifted_q 调用的薄封装——实现者读取现 build_ticket 的 base 段与 freq_legs 内部逻辑后按同名抽取，行为不变；`PRED_DIR=ROOT/'data'/'03-predictions'` 顶部定义。）

**渲染规范（可读性硬规范·大哥 2026-08-30 要求）**：

```python
def render_ticket(t) -> str:
    """出票卡文本渲染——可读性硬规范：
    ①顶部摘要行(结构/seq/总成本/两档成本)；②每档一节、逐腿一行
      `编号 │ 联赛 │ 主 vs 客 │ 玩法 pick @赔率 │ EV`（列宽对齐，│分隔）；
    ③三池卡候选区每场两行(保底视角/翻身视角)；④旗标用 emoji 前缀(⚠分歧/🟡低置信)；
    ⑤结尾预算行(月翻身累计x/40·红线提示)；⑥与 v5.4.2 出票核对单同款式(编号│联赛│对阵)。
    开发者 sszhang"""
    lines = [f"┌ 阶梯出票卡 v2 · seq{t['seq']} · 总成本 {t['totalCost']}元 "
             f"(保底{t['tiers']['base']['cost']} + 翻身{t['tiers']['upset']['cost']}) ────────"]
    for name, tier in (("保底", t["tiers"]["base"]), ("翻身", t["tiers"]["upset"])):
        lines.append(f"│ {name}档 {tier['play'] if 'play' in tier else tier['shape']} · {tier['cost']}元 · {tier.get('note','')}")
        for l in tier.get("legs") or []:
            lines.append(f"│   {l['matchNumStr']} │ {l.get('match','')[:14]:14s} │ "
                         f"{l['play'].upper()} {l['pick']} @{l['odds']}" +
                         (f" │ EV{l['ev']:+.0%}" if "ev" in l else ""))
    lines.append("│ 三池推荐(A/B级场):")
    for c in t.get("cards") or []:
        if not c.get("candidates"):
            continue
        fl = ("⚠" if "divergence" in c["flags"] else "") + ("🟡" if "low_conf" in c["flags"] else "")
        rb, ru = c["rec_base"], c["rec_upset"]
        lines.append(f"│   {c['code']} {fl} 保底→{rb['pool'].upper()} {rb['pick']}(q{rb['q']:.0%})"
                     f" · 翻身→{ru['pool'].upper()} {ru['pick']}@{ru['odds']}")
    spend = upset_month_spend(str(date.today())[:7])
    warn = " ⚠月预算红线!" if spend >= MONTHLY_UPSET_CAP else ""
    lines.append(f"└ 翻身月预算: {spend:.0f}/{MONTHLY_UPSET_CAP}元{warn} · 出票核对单见 v5.4.2 格式")
    return "\n".join(lines)
```

- [ ] **Step 4: GREEN + main 接线**

main() 加参数：`--structure=new|legacy`（默认 new）；legacy 路径输出文件名改 `{date}-boldplay-legacy.json`；new 路径调 `build_two_tier` + `upset_month_spend` gate（超 MONTHLY_UPSET_CAP 关翻身档并提示）+ `render_ticket` 打印。集成 dry-run（真实在售）：

Run: `python3 boldplay.py --dry --structure=new`（若无 --dry 惯例则临时读缓存跑一次不落盘——以文件现状为准，报告注明方式）→ 人眼检查输出卡可读性。

- [ ] **Step 5: Commit** — `feat(boldplay-v2): 两档结构(保底4串11+翻身多池seq轮换)+月预算gate+可读性渲染`

---

### Task 5: settle 双形状适配

**Files:**
- Modify: `engine/scripts/boldplay.py`（settle 函数 + cmd_settle 扫描 glob 扩 legacy 文件）

**Interfaces:**
- Consumes: 现有 `settle(ticket, results)`（bets 结构结算）；backfill `option_hit` 判定口径
- Produces: settle 支持 `structure=="new"` 票（base.bets 11 注 expand ✓ 已是 bets 结构；upset.bets 2串1×3 独立判定）；cmd_settle 扫 `*-boldplay*.json`（含 -legacy）

- [ ] **Step 1: 失败 selftest**——构造 new 结构 fake ticket（base 4串11 中 2 腿 → 回款 1 注 2串1；upset pool-2x1x3 三注一注命中），断言 settle 的 payout/legHits。
- [ ] **Step 2: RED**
- [ ] **Step 3: 实现**——settle 对 `bets` 已通用（base 11 注/upset 3 注同构），主要工作：①腿判定支持 `play in ("crs","ttg","hafu")` 的 pick 文本（`leg_hit` from backfill 复用：market 前缀+pick）；②`pool-4x1` 倍数注；③cmd_settle 的 glob 从 `{date}-boldplay.json` 扩为 `{date}-boldplay*.json`。
- [ ] **Step 4: GREEN（selftest 断言 4串11 中2关回款=2×两腿赔率积）**
- [ ] **Step 5: Commit** — `feat(boldplay-v2): settle适配两档bets结构(4串11中2关回款/跨池2串1独立判定)+legacy文件扫描`

---

### Task 6: SKILL v5.5 + preference.json 纪律

**Files:**
- Modify: `skill/SKILL.md`（Step 6 阶梯出票卡节重写两档 + Step 7 三池卡输出规范 + 头部版本）
- Modify: `.claude/CLAUDE.md`（boldplay 描述行 + SKILL 版本号）
- Modify: `data/06-tickets/preference.json`（月预算+降仓位规则）

**Interfaces:**
- Consumes: Task 4 render_ticket 输出格式（SKILL 引用其样式）、`run.py` 无改动（boldplay 直调）

- [ ] **Step 1: SKILL.md Step 6 阶梯节重写**——核心文本：

```markdown
### 阶梯出票卡 ★ v5.5 两档制（v2 结构·2026-08-30 改造，证据=7版旧结构全灭-114 vs 容错实票+159.4）
- 生成：`python engine/scripts/boldplay.py [--structure=new|legacy]`（new 默认=两档；legacy 对照保留一个月→`{date}-boldplay-legacy.json`）
- **保底档**：HAD 4串11（6×2串1+4×3串1+1×4串1=22元）——63%命中率下回款概率85.2%，中2关回1注2串1（高赔腿覆盖全票=T002实证）；进阶档(5串1×3倍)砍除（7轮0/7全中）
- **翻身档**：多池引擎——逐场三池卡(CRS/TTG/HAFU)EV对比选腿，同场≤1腿；seq奇=跨池2串1×3注(容错·期望返还43.7%) / seq偶=跨池4串1×N倍(彩票·19.1%)；月预算40元(超则关档)；连续4轮0回款自动降半仓位
- **逐场三池玩法卡 ★ v5.5**：A/B级场次报告输出三池候选(q×赔率×EV)+双行推荐（保底视角=相近带q最高/翻身视角=赔率最高）+旗标（⚠CRS模型分歧旗·|q−市场隐含|>5pp降级第二 / 🟡低置信·小联赛不砍池走抽水兜底）
- **可读性硬规范**：出票卡文本=摘要行置顶+逐腿一行(编号│联赛│对阵│玩法pick@赔率│EV)+三池推荐区+月预算行——与 v5.4.2 出票核对单同款式
```

- [ ] **Step 2: Step 7 三池卡输出条目**（在第2层推荐场次详情规则处加：每场 A/B 级附三池玩法卡，`pools` 字段随预测 JSON 归档——corpus 透传不展开、attribute v1 仅记录不归因）
- [ ] **Step 3: preference.json**——`stake` 节加 `"upsetMonthlyCap": 40`；`notes` 加降仓位规则说明（连续4轮0回款降半·boldplay gate 自动执行）
- [ ] **Step 4: .claude/CLAUDE.md**——`engine/scripts/` 行的 boldplay 描述更新（两档制/三池卡）；skill/SKILL.md 行 v5.4→v5.5
- [ ] **Step 5: 验证 + Commit**——`grep -n "v5.5\|两档\|三池" skill/SKILL.md .claude/CLAUDE.md` 全命中；`python3 -c "import json; json.load(open('data/06-tickets/preference.json'))"` 合法。Commit: `docs(skill): v5.5两档制阶梯卡+逐场三池玩法卡+preference月预算纪律`

---

### Task 7: 集成验证 + freq_backtest 结构对照 + 可读性验收

**Files:**
- Modify: `engine/scripts/freq_backtest.py`（加 `--structure` 对照维度：保底档历史回放）
- Test: 集成实跑 + 保底回放

**Interfaces:**
- Consumes: 全部前序；历史轮 `{date}-boldplay*.json` 的 HAD 腿（在档可回放）；`{date}-boldplay-legacy.json`（T4 起 legacy 落此名）

- [ ] **Step 1: 保底档历史回放**——脚本段（freq_backtest.py 内新函数 `replay_base_structure()`）：扫历史 boldplay JSON 的 HAD 腿（base/mid 档四~五星腿），按 4串11 展开结算 vs 旧 4串1×2注，输出各轮对照（回款轮数/总派彩/期望返还）。注意 spec §4.4：**翻身多池不可历史回放**（无 pools 历史数据），回放仅保底档。
- [ ] **Step 2: 跑回放**——历史 7 轮：预期 4串11 至少若干轮"中2关回款"（vs 旧 0 回款）——输出对照表进脚本 stdout + 落 `data/04-summaries/` 不需要（对照随 trend 定夺期积累）。
- [ ] **Step 3: 真实 dry-run 验收**——`python3 boldplay.py --structure=new`（真实在售）：①JSON 结构合法；②render_ticket 输出**给大哥过目**（可读性验收=最终用户确认）；③`--structure=legacy` 同日跑确认独立文件不覆盖。
- [ ] **Step 4: 全量回归**——`cd engine && python3 -m pytest tests -q` 全绿（基线 178+新增）。
- [ ] **Step 5: Commit** — `feat(boldplay-v2): freq_backtest保底结构对照回放+集成验收`

---

## Self-Review 记录

- Spec 覆盖：§4.1 两档↔T4 / §4.2 聚合↔T1/T2 / §4.3 卡+旗↔T3 / §4.4 legacy+回测范围↔T4/T7 / §4.5#1-7↔T1-T7 逐项 / §5 错误边界↔T3(low_conf/no_candidates)+T4(腿不足关档/月预算gate) / §6 测试↔各任务+T7 / 可读性硬规范↔T4 渲染块+T7 验收。D8 α↔T2、D9 低置信↔T3、D10 双行↔T3、D11 文件分离↔T4/T5、D12 分歧旗↔T3。
- 占位符：`_pick_had_legs/_is_ab/_q_map_for` 标注为"现有逻辑薄封装·行为不变"并指明抽取来源（build_ticket base 段/freq_legs）——实现者需读源后抽取，非 TBD（名称/签名/职责已定）。`BAND_DEFAULT` 给出确切值与复用优先指令。
- 类型一致：`pools_card → cards → build_two_tier legs(q/ev 字段) → render_ticket 渲染键` 链路逐一核对一致；`hafu_alpha()["alpha"]` 与 `hafu_agg(alpha=...)` 参数名一致。
